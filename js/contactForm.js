const sendEmail = function (e) {
  /*   e.preventDefault();
    const name = document.querySelector("form #name").value;
    const email = document.querySelector("form #email").value;
    const subject = document.querySelector("form #subject").value;
    const messsage = document.querySelector("form #message").value;
/*     if (!(emailIsInCorrectFormat(email))) return false;
 */    /* window.location.href = "#"
    window.open(``, "_blank") */
   /*  window.open(`mailto:charlesobird@outlook.com?subject=${name} - ${subject} (${email})&body=${messsage}`,"_blank") */
    //window.location.assign(`mailto:charlesobird@outlook.com?subject=${name} - ${subject} (${email})&body=${messsage}`)  */

}